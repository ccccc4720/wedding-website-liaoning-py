# Microsoft Forms 婚礼回执管理教程（网站 v8）

## 一、目前已经完成的状态

婚礼网站已直接嵌入这份 Microsoft Forms：

`https://forms.cloud.microsoft/r/uDUBnBgJ24`

宾客进入网站的“出席回复”区域后，可以直接在网页内填写并提交。若浏览器无法显示内嵌表单，页面下方还有“打开 Microsoft Forms 回执”按钮，可改用新页面填写。

你不需要再编辑 `rsvp-config.js`，网站包内也已经移除该旧配置文件。

## 二、上线前必须测试

1. 双击打开 `index.html`，确认“出席回复”区域出现 Microsoft Forms。
2. 点击备用按钮，确认会打开同一份表单。
3. 使用浏览器无痕窗口测试，确认宾客不需要登入 Microsoft 帐号。
4. 填写一笔测试资料并提交。
5. 回到 Microsoft Forms 后台，打开该表单的“回复”页面，确认测试资料已经收到。
6. 用手机再测试一次。
7. 请一位中国大陆亲友使用当地手机网络测试表单是否可正常载入和提交。

## 三、确认表单允许宾客填写

在 Microsoft Forms 中打开这份表单：

1. 点击“收集回复”。
2. 将回复权限设为“任何人都可以回复”或英文界面的 `Anyone can respond`。
3. 不要选择“仅组织内人员可以回复”，否则多数婚礼宾客会被要求登入。
4. 再用无痕窗口打开公开网址确认。

## 四、查看与导出回复

1. 登入 Microsoft Forms。
2. 打开婚礼回执表单。
3. 点击“回复”或 `Responses`。
4. 页面会显示回复人数与各题统计。
5. 点击“在 Excel 中打开”或 `Open in Excel`，即可下载宾客名单。

建议在婚礼前定期下载一份 Excel 备份。

## 五、日后更换 Microsoft Forms

网站目前把表单网址直接写在 `index.html` 中。更换表单时：

1. 用文字编辑器打开 `index.html`。
2. 搜寻 `forms.cloud.microsoft`。
3. 你会找到两个位置：
   - iframe 的 `src`：页面内嵌表单。
   - 备用按钮的 `href`：新页面打开表单。
4. 两处都要换成新表单网址。
5. 保存后重新测试。

Microsoft Forms 的“收集回复”页面通常可以提供专用的嵌入代码。若取得该代码，应优先把其中 iframe 的 `src` 放进网站。

## 六、常见问题

### 网站内出现空白区域

先等待数秒。若仍为空白：

- 检查网络连接。
- 点击“打开 Microsoft Forms 回执”。
- 确认表单仍开放收集回复。
- 确认没有设为仅限组织内部填写。

### 宾客被要求登入

通常是表单回复权限设置错误。请改成“任何人都可以回复”，然后用无痕窗口重新测试。

### 网站可以看到表单，但无法提交

检查 Microsoft Forms 是否已关闭收集回复、是否设置了开始或结束日期，以及必填题目是否全部完成。

### 中国大陆网络载入较慢

不同地区和电信网络的连线表现可能不同。正式发出前，请务必请当地亲友以手机网络实测。网站已经保留新页面备用按钮，内嵌载入失败时仍可尝试直接打开表单。
